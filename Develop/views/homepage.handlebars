{{#each posts as |post| }}
<div class="row mb-4 post">
  <div class="col-md-5">
    <h2>
      <a href="/api/posts/{{post.id}}">{{post.title}}</a>
    </h2>
    <p>By <a href="/api/users/{{post.user.id}}">{{post.user.name}}</a> on {{format_date post.date_created}}</p>
  </div>
</div>
{{/each}}